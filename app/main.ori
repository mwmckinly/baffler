type person {
   name: str,
   age: num
};

var students = [
   { name = "peter", age = 19 },
   { name = "max", age = 17 },
   { name = "jesse", age = 15 },
];

students = students + { name = "jesse", age = 15 };

disp(students);